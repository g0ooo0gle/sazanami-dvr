# 変更履歴

ここには過去のリリース情報を記載しています。現在の対応範囲と確認済みの条件は[互換実装表](docs/compatibility.md)を参照してください。

## v1.3.1 (2026-09-19)

- 番組名・説明・放送局名の一部が「�」でも、番組表更新と初期設定を続けられるようにしました。v1.3.0からDB形式と設定の変更はありません。

## v1.3.0

- Mirakurun URLを指定する`setup`コマンドで、DBの初期化、番組表の取得、チャンネル設定の生成をまとめて行えるようになりました。既存のチャンネル設定は上書きしません。
- 古い番組表の履歴を自動で整理します。最新の番組表と予約・録画から参照される情報は残し、録画ファイルは削除しません。
- KonomiTVの設定例は、ライブ映像をMirakurunから直接受け取る`always_receive_tv_from_mirakurun: true`を既定にしました。
- サービス情報が見つからない番組を未解決として保持し、ほかの番組表の同期を続けられるようにしました。

DB形式は第14版へ更新されます。既存環境ではサービスを停止し、バックアップを作成してから`db migrate`を実行してください。
`setup`は既存DBの形式を自動更新しません。詳しくは[更新・切り戻し・削除](docs/operations/update-and-remove.md)を参照してください。

## v1.2.0

READMEを整理しました。最初の画面から、Linux、Docker Compose、KonomiTVの導入手順へ直接進めます。詳しい情報は、導入、操作、運用、仕様、トラブル対応、開発者向けに分けました。

以前の文書パスは残し、新しい参照先を案内します。製品機能、公開API、DB形式、Compose構成はv1.1.0と同じです。

## v1.1.0

Linux配布アーカイブに、systemd向けのインストーラを追加しました。アーカイブを展開して`sudo ./packaging/install.sh install`を実行すると、専用利用者、標準ディレクトリ、実行ファイル、サービス定義、環境設定ファイルを配置します。DBの更新、番組表の取得、チャンネル設定、サービスの起動は利用者が確認してから行います。

同じインストーラから、設定・DB・録画・バックアップを残す通常削除と、確認付きの完全削除を実行できます。完全削除は対象の所有者、種類、リンク、mount、実行中プロセスを変更前と確認後に検査し、管理対象を確定できない場合は何も削除せず終了します。標準外の録画先は自動で削除しません。

READMEと導入ガイドには、Linuxへ直接導入する方法と、既存のDocker Compose構成を使う方法の短い入口を追加しました。製品機能、公開API、DB形式、Compose構成はv1.0.0から変更していません。

## v1.0.0

v1.0.0は、固定KonomiTV v0.14.1向け録画バックエンドの最初の安定版です。番組表、検索、予約、放送途中の録画、録画中の停止、自然終了、自動予約、ライブ視聴、完成録画の閲覧・再生・再解析・削除が対象です。

公開条件は、通常の自動テストと、初期状態のUbuntu環境で導入・更新・復元・切り戻し・通常削除・purgeを一回通すことです。Linux配布アーカイブとOCIイメージは、amd64／arm64の2アーキテクチャ向けに提供します。

DB形式は第13版のままで、v0.5.0から形式の変更はありません。更新前にバックアップを作り、更新後に`db status`が`CURRENT`であることを確認してください。詳しい対応範囲と既知の制限は[互換実装表](docs/compatibility.md)を参照してください。

## v0.5.0

KonomiTV v0.14.1を対象に、番組情報と設定、予約、放送途中からの録画、録画中停止、自然終了、自動予約API、ライブ、完成録画の再生・再解析・サムネイル再生成・削除を、一連の操作として確認しました。
確認対象はKonomiTV v0.14.1に固定しています。画面と検索条件を全件総当たりする試験ではありません。このクライアントから届く要求と、公開APIの後続状態を確認しています。

番組内容がAからBへ変わった後にAへ戻っても、過去の番組リビジョンを再利用して番組表の更新を続けます。
失敗した更新候補は、次の同一性判定には使いません。
Docker Composeの設定例はv0.5.0のイメージにそろえました。

DB形式は第13版のままで、v0.3.0からのデータベース形式の更新はありません。
更新前にバックアップを作り、更新後に`db status`が`CURRENT`であることを確認してください。

EDCB全機能、KonomiTVの他の版、Komorebi全体、実機で全画面・全検索条件を総当たりする試験は対象外です。
詳しい確認範囲と既知の制限は[互換実装表](docs/compatibility.md)を参照してください。

## v0.3.0

放送中に追加した予約は、追加した時点から録画を始められるようになりました。
対象は、予約を追加してから5分以内で、番組の残り時間が60秒以上ある場合です。
すでに開始時刻を過ぎた予約を、再起動後にまとめて録画することはありません。
録画中の予約は、これまでどおりKonomiTVから取り消し、停止時点までの部分録画として残せます。

Linuxでは、Sazanami DVRとKonomiTV v0.14.1をDocker Composeから起動できます。
SazanamiのLinux amd64／arm64イメージは、版タグでGHCRへ公開します。
KonomiTVは、固定したソースの公式Dockerfileからローカルビルドします。
Mirakurun／mirakc、チューナー、ドライバー、カードはホスト側の既存環境を使います。
既存の配布アーカイブとsystemdによる導入も引き続き利用できます。
詳しい手順と制限は[Docker Composeで導入する](docs/docker-compose.md)を参照してください。

DB形式は第13版のままです。
v0.2.0のデータと録画をそのまま引き継げます。
更新前にバックアップを作り、更新後に`db status`が`CURRENT`であることを確認してください。

## v0.2.0

Komorebiから完成録画を原画質のままHTTPで再生できるようになりました。
非直接ライブ視聴では、元のTSを変換せずHLSで配信します。
ワンセグ別出力を有効にした予約では、本編を優先してワンセグを別ファイルへ保存します。
Mirakurunで利用できない強制チューナー指定は、無視せずに予約を作成できない理由を表示します。

DB形式は第12版から第13版へ更新されます。
v0.1.2から更新する場合は、`db migrate`の前にバックアップを作成し、そのIDを控えてください。
第13版から第12版へ戻す逆向きの更新処理はありません。
v0.1.2へ切り戻す場合は、v0.2.0の実行ファイルで更新前のバックアップを復元します。
DBの状態を確認してからv0.1.2を起動してください。
録画ファイルは削除、移動、上書きしません。

## v0.1.2

Ubuntu向けの標準設定例で、チャンネル設定の配置先が実装の条件と異なっていた問題を修正しました。チャンネル設定をデータ保存先の直下へそろえ、導入手順とCIの確認も同じ値に統一しています。録画機能、公開API、DB形式、チャンネル設定形式は変わりません。

## v0.1.1

Linux arm64配布物に、ソースが変更済みだと誤って記録される問題を修正しました。配布前に各バイナリの版、commit、変更状態、CGO、OS、CPUを確認し、不一致があれば公開を止めます。録画機能、公開API、DB形式は変わりません。

## v0.1.0

既定の待受ポートは、CtrlCmd 4520、録画履歴HTTP 4521、WebUI 4522です。
同時録画数を省略すると、起動時にMirakurunの設定済みチューナー数を一度取得します。
Linux向け配布物にはsystemd設定例と、導入、更新、切り戻し、データを残す通常削除の手順を追加しました。
番組追従が有効な録画では、放送中に終了時刻が短縮された場合や開始時刻が変更された場合も、終了予定へ反映します。
従来の延長だけを追従する動作に戻す場合は、`--active-follow-extension-only`を指定できます。

## v0.0.29

大きなDBでも、更新前バックアップと形式更新を続けて完了できるよう、DB保守操作の待ち時間を最長1時間にしました。
状態確認は10秒で終了します。
終了操作を受けた場合は、期限を待たずに中止します。

## v0.0.28

KonomiTVの録画後設定で、待機、待機からの復帰後再起動、休止、休止からの復帰後再起動、電源断を選べるようにしました。
完成ファイル、DB、録画後スクリプトを確定してから、ほかの録画がないことを確認し、一度だけ実行します。
次の予約まで10分未満の場合や異なる指定が重なった場合は実行しません。
DB形式は第12版へ更新します。

## v0.0.27

自動予約の「録画済み番組名を確認する」条件に対応しました。指定日数内に完全に同じ番組名の録画履歴がある場合は、同じチャンネルまたは全チャンネルの指定に従い、予約を無効状態で残します。既に作られた自動予約も、録画開始前なら無効へ変えます。DB形式は変わりません。

## v0.0.26

番組検索と自動予約の条件判定を共通化しました。自動予約でも、番組詳細、ジャンルと除外、あいまい検索、映像・音声を評価できます。DB形式と保存済みの条件は変わりません。

## v0.0.25

予約の追加・変更・取消しが成功したときの返答を、KonomiTVとKomorebiの両方が読める共通形式へそろえました。予約の保存処理やデータベース形式は変えていません。

## v0.0.24

Sazanami DVRは、Komorebi 1.1.0-beta6が送る、複数チャンネルをまとめて指定する番組表要求に対応しました。
公式APKをテレビ向け1080p画面のAndroid環境で動かし、短時間用と全期間の番組表更新、番組表画面、予約画面を確認しています。
応答ヘッダーの読取り失敗、接続拒否、致命的なエラーは0件でした。
Android TV実機またはAndroid TV専用のシステムイメージでは、まだ確認していません。

## v0.0.23

保存先とファイル名を指定した予約をKonomiTVから変更できない問題を修正しました。KonomiTVが予約一覧から送り返す予定ファイル名は互換用の値として読み取り、保存先には使いません。保存先は、従来どおり検証済みの録画フォルダー設定だけから決めます。

## v0.0.22

KonomiTVの「デフォルト」「何もしない」と録画後スクリプトを予約へ保存できるようにしました。
スクリプトは`<data-root>/post-recording-scripts`内の実行可能ファイルだけを対象にし、録画とDBの確定後に一度だけ実行します。
シェルや自動再試行は使わず、5分で終了します。
スクリプトが失敗しても完成した録画は残ります。
この時点では、待機、休止、再起動、電源断は未対応でした。

## v0.0.21

KonomiTVの字幕・データ放送設定を予約へ保存し、実際の録画TSへ反映できるようにしました。
既定は字幕あり、データ放送なしです。
字幕またはデータ放送のいずれかを除外する録画では、PATとPMTを小さい上限内で読み、PMTと対象PIDを整合させたまま保存します。
TS形式を安全に確認できない場合は、その録画を`STREAM_FORMAT_INVALID`で失敗させます。

## v0.0.20

利用者の`umask`が0002の場合も、SQLiteのWALとSHMを0600へそろえるようにしました。
DB更新前のバックアップを、一般的なUbuntu環境でも、厳格な権限検査を維持したまま復元できます。
DB形式と保存内容はv0.0.19から変わりません。

## v0.0.19

KonomiTVの予約画面で指定した録画先フォルダーとファイル名テンプレートを、実際の録画ファイルへ反映できるようにしました。
録画先には`--recording-root`からの相対フォルダーだけを指定できます。
絶対パスや親フォルダーへの参照は拒否します。
番組名、放送局名、開始・終了日時、放送時間、放送ID、予約番号の代表的なEDCBマクロをGoだけで展開します。
単発予約と自動予約で同じ設定を使います。

## v0.0.18

KonomiTVとKomorebiの局ロゴを取得できるようにしました。
完成済み番組表と一致する放送局だけをMirakurunへ問い合わせます。
画像はSazanamiへ保存しません。
ロゴがない場合や通信に失敗した場合は、番組表や録画を止めずに各クライアントの標準画像に切り替えます。
実験環境では、KonomiTVとKomorebi相当の取得結果が一致し、KonomiTVのロゴAPIも正常に応答しました。

## v0.0.17

CtrlCmdを追加設定なしで同じLANから使えるようにしました。
`recording serve`と`ctrlcmd serve`は、既定でIPv4の全インターフェースで待ち受けます。
認証はないためインターネットへ直接公開しないでください。
同じPCだけに絞る場合は`--listen 127.0.0.1:4510`を指定します。
録画再生HTTPとWebUIは、従来どおり同じPCからだけ接続できる既定です。

## v0.0.16

KonomiTVの基本録画設定に対応しました。予約の有効・無効、優先度1～5、番組追従、既定余白、前後それぞれ-3,600～3,600秒の個別余白を、追加・一覧・変更で同じ値のまま扱います。既定では番組開始5秒前から番組終了2秒後まで録画します。無効予約は録画せず一覧に残り、開始前なら再び有効にできます。自動予約から作る予約にも同じ設定を引き継ぎます。

## v0.0.15

KonomiTVから録画中の予約を取り消したとき、その一件だけを停止できるようにしました。
188バイト以上を保存できていれば通常の`.ts`名で公開し、利用者が途中で止めた部分録画だとDBへ残します。
通常終了とは区別したまま、CtrlCmdの録画一覧、録画履歴API、HTTP Range再生から読み出せます。

## v0.0.12～v0.0.14

**注意:** v0.0.13のジャンル条件には誤りがあるため、使用しないでください。

Mirakurunから番組詳細、ジャンル、映像、音声を取得し、番組表へ保存する機能を追加しました。
KonomiTVの番組表示と検索へ同じ情報を返します。
ジャンル検索、ジャンル除外、映像・音声の絞り込み、表記の違いや小さな入力違いを許すあいまい検索に対応しています。
v0.0.14では、KonomiTVが実際に送るバイト順へ戻しました。
そのバイト順を固定値で検査しています。

## v0.0.11

KonomiTVの番組検索に対応しました。検索のたびにMirakurunへ接続せず、最後に正常取得できた番組表を256件ずつ読みます。ジャンルとあいまい検索は、条件を無視して誤った結果を返さないよう、この版では未対応として扱います。

## v0.0.10

KonomiTVとKomorebiが使うライブ視聴の開始、中継、終了を追加しました。
KonomiTV v0.14.1のライブAPIで25秒間の受信を確認しました。
直接接続は、2本同時に接続する試験と、20回繰り返す試験の両方に成功しました。
Android TV上のKomorebi画面は未確認です。
詳しくは[互換実装表](docs/compatibility.md)を参照してください。

## v0.0.9

録画履歴、Komorebi向け一覧・詳細、直接再生、シークを追加しました。これらの操作は自動テスト済みですが、Android TV実機ではまだ確認していません。
