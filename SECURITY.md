# セキュリティポリシー

## 対象バージョン

現在は、公開済みの最新ベータ版だけを対象とします。古いバージョンへの修正提供は保証していません。

## 脆弱性の報告

脆弱性や秘密情報の混入を見つけた場合は、公開Issueへ詳細を書かず、GitHubのPrivate vulnerability reportingから報告してください。確認したバージョンまたはコミット、影響、再現に必要な最小限の情報を添えてください。

Private vulnerability reportingを利用できない場合は、詳細を書かずに連絡方法を尋ねるIssueを作成してください。実際の放送TS、認証情報、家庭内ネットワークの情報は送らないでください。

## 現在の安全境界

- CtrlCmdは既定で`0.0.0.0:4520`に待ち受ける
- 録画履歴HTTPは`127.0.0.1:4521`、WebUIは`127.0.0.1:4522`に待ち受ける
- 録画履歴HTTPは、明示設定でprivate IPまたは全インターフェースへ待ち受けられる
- 認証機能はなく、同じPCまたは信頼できる宅内LANで使う構成を前提とする
- リクエスト、応答、文字列、同時接続、処理時間に上限を設ける
- Mirakurunの接続先に認証情報を埋め込まない
- HTTPリダイレクト、環境変数によるプロキシ、TLS検証の無効化を行わない
- DBと録画ファイルは、所有者だけが読み書きできる保存先に置く
- 既存の完成録画を上書きせず、途中ファイルを完成ファイルと区別する

LAN待受を使う場合は、ホスト側ファイアウォールとルーターで接続元を宅内に限定してください。インターネットへ直接公開しないでください。複数ユーザーが同じPCを使う環境や、信頼できないローカルプロセスが動く環境も、現在の想定範囲外です。
