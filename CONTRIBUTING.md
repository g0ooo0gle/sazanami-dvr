# Sazanami DVRへの貢献

不具合報告、動作環境の情報、コードの改善を歓迎します。初期プレビューのため、大きな機能を追加する前にIssueで目的と範囲を相談してください。

## 開発の流れ

このリポジトリはGitHub Flowで運用します。

1. `main`から短期間だけ使う作業ブランチを作る
2. 一つの目的に絞って変更する
3. テストを実行する
4. ブランチをpushし、プルリクエストを作る
5. CIとレビューが通ったら`main`へマージする
6. マージ後は作業ブランチを削除する

公開リポジトリで直接`main`へforce pushしません。履歴の修正が必要な場合は、影響と復旧方法を確認してから管理者が行います。

## 設計上の原則

- `internal/core`へCtrlCmd、HTTP、SQL、ファイル形式、特定の接続先に依存する型を持ち込まない
- 番組情報、放送ストリーム、稼働確認の役割を分ける
- 番組情報やTS全体をメモリへ読み込まず、上限付きで処理する
- キュー、バッファ、goroutine、リクエスト、文字列に上限を設ける
- 内部時刻はUTCとし、外部形式との境界で必要なタイムゾーンへ変換する
- 外部入力は、状態変更や応答出力より前に検証する
- ネットワーク待受はローカルループバックに限定する

まず標準ライブラリで実現できるか検討し、必要性が明確でない抽象化や依存関係は追加しません。

## コードとコメント

一つの関数へ通信形式の変換、業務判断、入出力を詰め込まないでください。公開する型、インターフェース、関数、メソッドには、役割や守るべき条件が分かる日本語のGoDocコメントを付けます。

## テスト

変更箇所に応じて、正常系だけでなく、上限値、不正入力、キャンセル、タイムアウト、部分的な入出力、リソース解放、並行実行も確認します。自動テストから実Mirakurun、チューナー、家庭内データへ接続してはいけません。

プルリクエストを送る前に、Go 1.26.5で次を実行してください。

```console
gofmt -w cmd internal
go test -count=1 ./...
go test -count=1 -shuffle=on ./...
go test -count=1 -race ./...
go vet ./...
CGO_ENABLED=0 go build -trimpath ./cmd/sazanami-dvr
```

## 公開してはいけない情報

認証情報、家庭内のIPアドレスやホスト名、個人情報を含むパス、実際の予約・番組情報・放送TS、カードや鍵の情報をコミットしないでください。外部のコードやデータを使う場合は、出典とライセンスを確認します。

脆弱性は公開Issueへ書かず、[SECURITY.md](SECURITY.md)に従って報告してください。
